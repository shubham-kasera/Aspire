import React from 'react';
import {View, Text} from 'react-native';

const Payments = () => {
  return (
    <>
      <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
        <Text style={{color: '#fff'}}>Payments Screen</Text>
      </View>
    </>
  );
};

export default Payments;
